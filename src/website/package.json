{
	"name": "bing-wallpaper-archive",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npm run build && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build:ts": "tsc",
		"build": "python build_website.py && npm run build:ts",
		"type-check": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "^5.9.3"
	}
}
